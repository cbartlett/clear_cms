ClearCms.User = DS.Model.extend({
  primaryKey: '_id',
  <% ClearCMS::User.fields.each do |field_name, field_options| %>
  <% if field_name!='_id' %>
    <%= field_name %>: DS.attr('string'),
  <% end %>
  <% end %>

});
